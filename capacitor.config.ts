import { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'com.talib.aistudio',
  appName: 'Talib AI Studio',
  webDir: 'frontend/dist',
  bundledWebRuntime: false
}

export default config
