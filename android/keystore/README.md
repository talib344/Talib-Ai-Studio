# Debug keystore (auto-generated placeholder)
# IMPORTANT: In a real project you should keep keystore secure and not commit the real keystore.
# For CI debug builds we'll generate a keystore in the workflow.
